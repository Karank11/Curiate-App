<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipToPadding="false"
    tools:context=".ui.savedscreen.SavedScreenFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/saved_content_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:listitem="@layout/saved_content_card"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <ProgressBar
        android:id="@+id/progressbar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <ImageView
        android:id="@+id/empty_image"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/ic_no_post"
        android:layout_marginHorizontal="32dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/empty_title"
        app:layout_constraintVertical_chainStyle="packed"/>

    <TextView
        android:id="@+id/empty_title"
        android:textAppearance="@style/TextAppearance.Material3.TitleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="@string/empty_title"
        android:textColor="?attr/colorOnSurface"
        android:fontFamily="serif"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="@id/empty_image"
        app:layout_constraintEnd_toEndOf="@id/empty_image"
        app:layout_constraintTop_toBottomOf="@id/empty_image"
        app:layout_constraintBottom_toTopOf="@id/empty_description"/>

    <TextView
        android:id="@+id/empty_description"
        android:textAppearance="@style/TextAppearance.Material3.BodyMedium"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginHorizontal="32dp"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:gravity="center"
        android:text="@string/empty_description"
        app:layout_constraintStart_toStartOf="@id/empty_image"
        app:layout_constraintEnd_toEndOf="@id/empty_image"
        app:layout_constraintTop_toBottomOf="@id/empty_title"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <androidx.constraintlayout.widget.Group
        android:id="@+id/empty_group"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="visible"
        app:constraint_referenced_ids="empty_image, empty_title, empty_description"
        tools:visibility="visible"/>

    <androidx.constraintlayout.widget.Group
        android:id="@+id/posts_group"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:constraint_referenced_ids="saved_content_recycler_view, progressbar"
        tools:visibility="gone" />

</androidx.constraintlayout.widget.ConstraintLayout>